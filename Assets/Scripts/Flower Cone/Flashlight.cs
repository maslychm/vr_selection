using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Flashlight : MonoBehaviour
{
    /*
    [SerializeField] private Light flashlight;

    [SerializeField] private float sizeRate = 1.5f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        float thumbstick = OVRInput.Get(OVRInput.Axis2D.PrimaryThumbstick);
        // bool leftTrigger = OVRInput.Get(OVRInput.RawAxis1D.LIndexTrigger);
        flashlightSize(thumbstick);
    }

    // Size spot angle changes based on forward/backward movement of right joystick
    public void flashlightSize(float joystick)
    {
        if (joystick == 1.0f)
        {
            flashlight.spotAngle = Time.deltaTime * sizeRate;
        }
        else if (joystick == -1.0f)
        {
            flashlight.spotAngle = Time.deltaTime / sizeRate;
        }
    }
    */
}
